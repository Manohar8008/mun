# Base URL of the Munin backend API. Only needed if the backend isn't
# running at the default http://localhost:4000/api (e.g. a different port,
# or a deployed backend). Copy to .env and uncomment to override.
# VITE_API_BASE=http://localhost:4000/api
